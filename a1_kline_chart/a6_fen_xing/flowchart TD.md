```mermaid
flowchart TD
    Start([Start])
    InitPivot[Initialize Pivot()]
    CheckBottomFractal{Is current anchor a bottom fractal?}
    CheckOverlap{Overlap condition met?}
    SetPivotPoints[Set pivot.bg, pivot.highly, pivot.lowly]


```